{% extends "base.html.twig" %}

{% block body %}


    {# {{dump(user)}} #}

    {% if error == 1 %}
        <p>Erreur impossible de vous trouver !</p>
    {% elseif error == 2 %}
        <p>Votre compte est déjà validé !</p>
    {% else %}
        <p>Vous êtes maintenant validé, vous pouvez vous <a href="{{path('user_login')}}">Connecter</a></p>
    {% endif %}
    
    

{% endblock %}